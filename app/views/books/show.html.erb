<h2>Show</h2>
<%= @book.title %> <br/>
<%= @book.author %> <br/>
<%= @book.category.name %> <br/>
<%= @book.description %>

<% if user_signed_in? %>
    <% if @book.user_id == current_user.id %>
    <%= link_to 'Edit', edit_book_path(@book) %>
    <%= link_to 'Delete', book_path(@book), method: :delete, data: {confirm: 'Are you sure?'} %>
    <% end %>
<% end %>